/**
 * Адреса каталога, безопасные для клиентских компонентов: модуль без
 * runtime-импортов данных (taxonomy тянет queries → весь сид в бандл).
 */

/** Канонический адрес карточки товара. */
export const productHref = (id: string) => `/catalog/tovar/${id}`;
